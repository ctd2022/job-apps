# FastAPI Backend Dependencies
# Job Application Workflow - Track 2: Local Web UI

# Core FastAPI
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6  # For file uploads

# Pydantic for data validation (comes with FastAPI but explicit is better)
pydantic>=2.5.0

# WebSockets support (for future real-time updates)
websockets>=12.0

# CORS and other middleware
starlette>=0.27.0

# Already in your existing requirements (no need to reinstall):
# - ollama
# - python-docx
# - PyPDF2
# - requests
# - markdown

# Optional: For future SQLite database support
# sqlalchemy>=2.0.0
# aiosqlite>=0.19.0

# Optional: For Celery task queue (future enhancement)
# celery>=5.3.0
# redis>=5.0.0
